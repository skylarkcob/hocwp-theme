<?php
if ( ! defined( 'ABSPATH' ) ) {
	exit; // Exit if accessed directly
}

if ( ! class_exists( 'HT_Constants' ) ) {
	class HT_Constants {
		// Custom post type

		// Custom taxonomy

		// Rewrite slug

		// Post meta

		// Term meta
	}
}